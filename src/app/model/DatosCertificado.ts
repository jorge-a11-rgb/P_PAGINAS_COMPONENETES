import { RespCertificado } from "./RespCertificado";

export class DatosCertificado {
    public certificado: string;
    public fechaObtencion: '';
    public respCertificado: RespCertificado = new RespCertificado(0, '');
    public fechaVencimiento: '';
    
  
  // eslint-disable-next-line eol-last
  }
  