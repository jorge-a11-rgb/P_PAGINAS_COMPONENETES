import { RespExp } from "./RespExp";

export class DatosExperienciaLaboral {
    public empresa: string;
    public annoInicio: number;
    public respExp: RespExp = new RespExp(0, '');
    public annoTermino: number;
    public cargo: string;
  
  // eslint-disable-next-line eol-last
  }
  